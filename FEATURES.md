| # | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Done |
|---|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----|
| 1 | The HTML UI has button Alert for sending the Alert message.<br>Add ability to send Registration and StatusReport messages by clicking a button too.<br>Add buttons for Registration and StatusReport to the HTML UI.<br>These buttons must have backend code look as close as possible similar to the Alert backend code.<br>These buttons must be in the following order from left to right: Registration, StatusReport, Alert.<br>These buttons must be disabled when no respective object (node returns nil) same as Alert.                                                                                                                                                                                                                                                                                                                                                                                                                                        | ✅ |
| 2 | Ability to reload fixtures when clicking a button to send a message "manually" (Send Registration, Send Status Report, Send Alert).<br>Fixtures must be reloaded before the message is sent.<br>This ability is optional — controlled by a toggle "Auto Reload On Manual Send" positioned near the Reload button, with a concise tooltip explaining it forces fixture reload only for manual sends.<br>Default value is false.<br>API code must look similar to the node online status switch.<br>The backend stores the Auto Reload On Manual Send state in EdgeNodeRegistry.<br>This setting relates to all nodes. Not per each node.<br>NodeController checks this state and calls EdgeNodeRegistry to reload fixtures when processing a manual send request.<br>Auto Reload On Manual Send must have no impact on automatic lifecycle message sending.                                                                                                            | ✅ |
| 3 | Nodes must be loaded from fixtures in "offline" state. User must set them online explicitly. State of already loaded nodes must stay intact on reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |  |
| 4 | Ability to send detection reports. I need to be able to discover fixture of detection report (detection_report.json). The UI must have the Send Detection button. Next to Send Alert. I want it to be of a red color. If Detection Report is not present for a node - the Send Detection button is disabled. When clicking Send Detection - backend invokes existing method of a Dispatcher to pass a Detection Report. If the Detection Report fixture has no reportId set, the backend auto-generates a ULID and assigns it before sending, same way as StatusReport and Alert do for their respective IDs.                                                                                                                                                                                                                                                                                                                                                         | ✅ |
| 5 | The UI displays two separate live badges for the fusion node: **connection state** (Connected / Connecting… / Disconnected / Closed) and **reachability** (Reachable / Unreachable). Both are pushed from the backend via SSE — no polling. The backend stores the last known state of each and probes on startup so a freshly opened UI immediately shows current values. Connection state is driven by `IClient.addStateChangeListener`. Reachability is re-probed via `probeReachable` on each `DISCONNECTED` transition and set to `true` on `CONNECTED`; `CONNECTING` and `CLOSED` transitions leave the last known reachability unchanged. Each badge is a separate named SSE event (`connection`, `reachability`) rendered with a Thymeleaf template fragment and broadcast to all open streams. A **"↻ Probe" button** lets the user trigger a manual reachability check at any time; the result updates `lastReachable` and is broadcast to all subscribers. | ✅ |
| 6 | The UI displays each node's Registration `short_name` as its label; if `short_name` is empty or missing, fall back to `name`. Applies everywhere a node is identified in the UI (list rows, headings, badges, etc.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ |
| 7 | The UI shows one-level parent/child relationships between edge nodes derived from each Registration's `dependent_nodes` list. The backend returns list of nodes with boolean flag indicating is node parent or dependent. The backend sorts all nodes so dependent nodes always come after their parent nodes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | ✅ |